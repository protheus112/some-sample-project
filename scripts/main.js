const myHeading = document.querySelector('h1'); // declaring the h1 in a variable
//myHeading.textContent = "Hello world!"; //fetching the variable, declaring it's value
/* comment on multiple lines
like in the case of css and html */
let firstTest = 'vanilla';
if(firstTest === 'vanilla'){
    myHeading.textContent = "Test OK";
    
} else {
    myHeading.textContent = "Not OK"
}